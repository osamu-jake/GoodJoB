# 設計 — 知の越境検索（PROJECT ZERO / W6）

仕様を「どう作るか」に落とす。仕様に無いものは足さない。各項目に根拠（仕様の見出しか要件ID）を付ける。
前提：Streamlit単一プロセスのPoC。Next.js/FastAPIのような層分離はしない（仕様「全体像」）。

## 構成

```
app.py                      -- エントリーポイント。起動時にDB自動構築を呼び出し、st.tabs()でタブ①②を切替
ui/
  tab_search.py              -- タブ①検索：検索窓・検索方式トグル・カードリスト描画。
                                 `query_side`は'need'固定（方向は切り替えない。ADR-0028）
  tab_techdb.py               -- タブ②技術データベース：一覧・絞り込み・詳細画面。詳細では
                                 「似た課題を解く技術＋その実績・評価」を出す（ADR-0029）
  cards.py                    -- 検索結果カード1件分の描画（関連度%・要約・ハイライト・権利状況・実績）
  detail.py                   -- 技術1件の詳細。`st.dialog`のモーダルとして開き、タブ①②で共用（ADR-0032）
search/
  cross_search.py             -- cross_search()：BM25＋ベクトル取得→RRFで候補集合を決定。
                                 反対側（上位10件）と同じ側（最大3件）を別々に検索して別々に返す（ADR-0024）
  bm25.py                      -- FTS5（title/body/problem/background の4列、contentless、unicode61）
                                 での検索とスニペット取得（ADR-0027）
  tokenizer.py                  -- Janome＋品詞フィルタ＋ストップワードの分かち書き（ADR-0011）。
                                   索引を張る側（build_db）と検索する側（bm25）の両方が同じ関数を使う。
                                   ここがずれると「入れた語で引けない」不具合になるので1か所に置く
  vector.py                     -- multilingual-e5-smallのロード・エンコード・コサイン類似度計算。
                                   フィールド別ベクトルに対して計算し、doc_idごとに最大値へ集約（ADR-0021）
  rrf.py                         -- Reciprocal Rank Fusion（k=60）
  scoring.py                      -- 関連度%変換（相対値化。ADR-0010）
  hit_judge.py                     -- 「ヒットなし」判定（反対側BM25=0件 かつ 類似度<基準値。ADR-0008）
  rephrase.py                       -- 言い換え候補生成（F-21）
  highlight.py                       -- 一致理由の語句ハイライト（検索結果上位のみ都度計算。F-18、ADR-0018）
db/
  schema.sql                          -- documents/tech_usage/evaluations/patents/documents_fts/embeddings/
                                         search_logsの定義。documents_ftsはcontentless（ADR-0027）
  build_db.py                          -- schema.sql＋fixtures/seeds.sqlから起動時にDBを自動構築（.dbはGit管理外）。
                                          あわせて全文書のフィールド別ベクトルを計算しembeddingsへ保存
                                          （ローカル無料計算。ADR-0020・0021・0025）
  repositories.py                       -- 各テーブルへのCRUD関数群
  search_log.py                          -- search_logsへのINSERT（st.buttonブロック内でのみ呼ぶ）。
                                            標準ライブラリのloggingと名前が衝突するのでこの名前にした
ingest/
  summarize.py                             -- 【開発時に手動で1回だけ実行】seed文書の「平たく言うと」要約を
                                               生成し、knowledge.dbのsummary_plainへ書き戻す（ADR-0020・0025）
  crawler.py                                 -- requests＋BeautifulSoupでの定期クロール、content_hashで差分検知（F-11）
  local_folder_crawler.py                     -- ローカルフォルダ巡回デモ版（ダミーWord/PDF取り込み。F-12）
fixtures/
  seeds.sql                                     -- 手入力データの正本（documents/tech_usage/evaluations/
                                                   patents）。knowledge.dbから書き出したテキスト（ADR-0025）
tools/
  export_seeds.py                                -- knowledge.db → fixtures/seeds.sql の書き出し（コミット前に1回）
  check_gap.py                                    -- BM25が0件になるデモ用クエリの実測（N-06・U-04）
.github/workflows/crawl.yml                        -- 夜間バッチでcrawler.py→summarize.py→埋め込み再計算
                                                      （build_db.py）→DB更新（F-11）
tests/
  test_cross_search.py                                -- RRF統合、同じ側/反対側の分離、タブ①の方向固定（ADR-0028）
  test_hit_judge.py                                     -- ヒットなし判定の閾値テスト、デモ文でBM25=0件になることの確認（ADR-0011）
  test_scoring.py                                         -- 関連度%変換のテスト
```

**〔未〕が付いているものは2026-09-26時点でまだ存在しない**（計画のみ）:
`ingest/summarize.py`〔未〕／`ingest/crawler.py`〔未〕／`ingest/local_folder_crawler.py`〔未〕／
`search/highlight.py`〔未〕／`ui/detail.py`〔未〕（ADR-0032・担当ぐんさん）／`tests/`一式〔未〕／
`.github/workflows/crawl.yml`〔未〕。**実在するかどうかは実装計画の状態欄（✅／⬜）が正本。**

初版から減らしたファイルと、その理由（PoC実装で判明。ADR-0025）:

| 消したファイル | どこへ行ったか | なぜ |
| --- | --- | --- |
| `ingest/embed_batch.py` | `db/build_db.py` の埋め込み計算 | 埋め込みはDB構築と必ず同時に走る。別ファイルにすると「DBはあるがベクトルが無い」状態を作れてしまう |
| `ingest/patents_import.py` | 不要（`tools/export_seeds.py` が逆向きを担う） | ADR-0025で編集先を`knowledge.db`本体に変えたため、取り込む対象のファイルが無くなった |
| `db/logging.py` | `db/search_log.py`（改名のみ） | 標準ライブラリの`logging`と名前が衝突する |

## データ

- **手入力データの編集先は`knowledge.db`本体**（DBツールで直接編集）。コミット前に
  `tools/export_seeds.py`で`fixtures/seeds.sql`へ書き出し、そのテキストをGitに乗せる（ADR-0025）
- `knowledge.db`はリポジトリに含めない。`.db`が無いとき`db/build_db.py`が`schema.sql`と
  `fixtures/seeds.sql`から組み立てる。`.db`があるときは手元の編集を守るため何もしない。
  `git pull`後に取り込み直すときは`python3 db/build_db.py --rebuild`（ADR-0015・0025）
- 埋め込みベクトルは`db/build_db.py`がDB構築と同時に一括計算し、`embeddings.vec`にBLOB
  （`numpy.tobytes()`）で保持する。検索時にクエリ側のベクトルだけその場で計算する（仕様「入力と出力」F-02）
- `search_logs`はアプリ実行中のプロセス内でのみ確実に残る（Streamlit Community Cloudの制約。仕様
  「保存先の方針」）。デモ手順書に「アプリを再起動しない」旨を明記する
- シーズ文書（`source='patent_manual'`）・ニーズ文書（`source='dummy'/'faq'`）はどちらも
  `knowledge.db`の`documents`へ直接入力し、`fixtures/seeds.sql`へ書き出して固定する（仕様
  「データソースの方針」、ADR-0025）。訴求の競合チェック（旧F-20）は対応しないため訴求文書は扱わない（ADR-0019）
- **`summary_plain`は`knowledge.db`に入れておく**（開発時に`summarize.py`を手動で1回実行して生成・
  レビューし、`seeds.sql`へ書き出す）。デプロイ・起動時にAPIは呼ばない（ADR-0020）
- **埋め込みベクトルは`fixtures/`には含めない。** DBを組み立てるたびに`db/build_db.py`が
  ローカルで再計算し`embeddings`へ保存する。ローカル無料計算のため毎回計算し直しても問題ない（ADR-0020）
- 一致理由ハイライト（F-18）の語句単位ベクトルは事前保存せず、検索結果上位の文書に対してのみ
  `search/highlight.py`が都度計算する（ADR-0018）

## 処理の流れ

**起動時**
`app.py`起動 → `db/build_db.py`が`knowledge.db`の有無を確認 → 無ければ`schema.sql`でテーブル作成
→ `fixtures/seeds.sql`を読み込み投入（`summary_plain`は生成済みテキストをそのまま入れる。APIは呼ばない）
→ 同じ`build_db.py`がFTS5索引を張り、全文書の埋め込みを計算して`embeddings`へ保存
（ローカル無料計算。ADR-0020・0021）→ `st.tabs()`でタブ①②を表示

`.db`が既にあるときは何もせず既存のDBを返す。手元で転記中のデータを消さないため。
`git pull`で新しい`seeds.sql`が来たときは`python3 db/build_db.py --rebuild`で作り直す（ADR-0025）。

**データ準備時（開発時に手動で1回。デプロイ前）**
特許の発明の名称・要約・背景技術・課題をJ-PlatPatから手動転記し**`knowledge.db`へ直接入力**
（ADR-0021・0025） → `ingest/summarize.py`を手動実行し一言要約を生成・レビュー →
`tools/export_seeds.py`で`fixtures/seeds.sql`へ書き出し → コミット（ADR-0020・0025）

**検索（タブ①、仕様「入力と出力」F-02〜F-07・F-16・F-18・F-21）**
1. 企画案の文章を入力し検索ボタン押下（`query_side`は`'need'`固定。立場の選択UIは無い。ADR-0028・0030）
3. `search/cross_search.py`が`bm25.py`（対象`doc_type`で上位50件）と`vector.py`（フィールド別ベクトル
   と比較し、doc_idごとに最大類似度を採ってから上位50件。ADR-0021）を実行
4. `search/rrf.py`で統合（k=60）→上位10件を**候補集合として選定**。表示順は後段で関連度の降順に並べ替える（ADR-0023）
4b. 同じ手順を**同じ側**に対しても実行し、最大3件を`same_side_hits`として別に返す（ADR-0024）。
   1つの順位表には混ぜない。以降の5〜8は反対側の結果に対して行う
5. `search/hit_judge.py`が「ヒットなし」を判定（反対側BM25=0件 かつ 類似度<基準値）
   - ヒットなしの場合も候補は空にしない → `rephrase.py`で言い換え候補を生成し、カードリスト上部に表示
     （仕様「タブ①検索」「エラー時の挙動」）
   - 裏側では`hit_count=0`で`db/search_log.py`が`search_logs`へ記録 → タブ②「まだ社内に無い技術」に
     反映される
6. `search/scoring.py`が関連度%に変換、`search/highlight.py`が語句ハイライトを算出
7. `ui/cards.py`が1件ずつカードを描画（一言要約・権利状況・使用実績はDBから結合取得）。
   カードリストの下に折りたたみを置き、①`same_side_hits`（似た悩み）と②反対側hitsの
   `repositories.get_tech_usage`（既存の訴求）を並べる（ADR-0024・0031）
8. `db/search_log.py`が`st.button`のブロック内で`search_logs`へINSERT（F-04。ボタン外に書かない）

**技術データベース（タブ②、仕様「入力と出力」F-05・F-08・F-17・F-19）**
1. 絞り込み条件（未使用の技術／特許ステータス／まだ社内に無い技術）を選択
2. `ui/tab_techdb.py`が`db/repositories.py`経由で`documents`/`patents`/`tech_usage`/`evaluations`/
   `search_logs`を条件でフィルタし一覧表示
3. 1件クリック→詳細画面：エビデンス（原文リンク・`evaluations`の評価済み／見送り／未評価・
   `patents`の関連特許。ADR-0022）・使われ方（`tech_usage`）を表示
4. さらに**「似た課題を解く技術と、その使われ方」**（ADR-0029）。`cross_search`を`query_side='seed'`で
   呼び`same_side_hits`を取り、各`doc_id`に`repositories.get_tech_usage`／`get_evaluations`を結合して
   並べる。**ここが「この技術は何に使えそうか」の根拠**で、判定はせず記録だけを出す
5. その下に「関連する収集済みの声」（反対方向の`cross_search`）。「母集団を代表しません」の注記と
   出典URLを必ず付ける（ADR-0028）。訴求の競合チェックは対応しない（ADR-0019）

**データ取り込み（バッチ、仕様「入力と出力」F-01・F-11・F-12）**
`.github/workflows/crawl.yml`が夜間に`ingest/crawler.py`（差分検知）→`ingest/summarize.py`
（seed側のみ・登録時1回）→`db/build_db.py`の埋め込み再計算、の順で実行しDBを更新する

## 仕様との対応

| 設計項目 | 根拠（仕様の見出し／要件ID） |
| --- | --- |
| `app.py`, `db/build_db.py` | 仕様「全体像」「保存先の方針」、N-04、ADR-0015・0020・0021・0025 |
| `search/cross_search.py`, `rrf.py` | 仕様「入力と出力」F-02、ADR-0002・0003・0006・0024 |
| `search/bm25.py`, `tokenizer.py` | 仕様「データ」`documents_fts`、ADR-0011・0027 |
| `search/vector.py` | 仕様「データ」`embeddings`、ADR-0004・0005 |
| `search/scoring.py` | ADR-0010・0023 |
| `search/hit_judge.py`, `rephrase.py` | 仕様「タブ①検索」「エラー時の挙動」、ADR-0008、F-21 |
| `search/highlight.py` | 仕様「タブ①検索」、F-18、ADR-0018 |
| `ui/tab_search.py`, `cards.py` | 仕様「画面・操作」タブ①、F-03・F-06・F-10、ADR-0026・0028 |
| `ui/tab_techdb.py` | 仕様「画面・操作」タブ②、F-17・F-08、ADR-0029 |
| `db/repositories.py`（tech_usage） | 仕様「データ」`tech_usage`、F-08、ADR-0007 |
| `db/repositories.py`（evaluations） | 仕様「データ」`evaluations`、F-05、ADR-0022 |
| `db/repositories.py`（patents） | 仕様「データ」`patents`、F-19、ADR-0012 |
| `db/search_log.py` | 仕様「入力と出力」F-04、Streamlitの再実行モデルの注意点 |
| `ingest/summarize.py` | F-16、ADR-0016 |
| `ingest/crawler.py` | F-11、ADR-0011（差分検知） |
| `ingest/local_folder_crawler.py` | F-12 |
| `tools/export_seeds.py`, `fixtures/seeds.sql` | 仕様「データソースの方針」、ADR-0012・0025 |
| `.github/workflows/crawl.yml` | F-11、仕様「全体像」（夜間バッチ） |
| `tests/test_hit_judge.py` | N-06、ADR-0011（デモ用検索文でBM25が0件になることの確認） |

## 仕様の矛盾・欠落（あれば。実装側で勝手に埋めず、PMへ返してADRを切る）
（なし。F-18の語句ベクトルの持ち方はADR-0018、F-20用データ収集経路の欠落はADR-0019（Won't採用）で解消済み）
