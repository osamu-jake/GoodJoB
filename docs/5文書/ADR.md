# ADR — 知の越境検索（PROJECT ZERO / W6）

ユーザーが決めたことを、決めた順に1行ずつ残す。各文書の「決定事項」には、ここのIDだけを書く。

| 項目 | 内容 |
|---|---|
| プロジェクト | 知の越境検索（想定企業：株式会社マンダム） |
| チーム | じゃけ（PM）／ぐんさん（フロント）／べー（バック） |
| Gitアカウント | じゃけ＝Osamu-Jake／ぐんさん＝Kozueg-44gh／べー＝TanobeYuya |
| 期間 | 実装着手 9/28（月）／MTG 9/30（水）／デモ完成 10/4（日）／発表 10/13（火） |
| 実装方針 | 3名ともClaude Codeを使用 |
| 本書の位置づけ | 要求・要件・仕様・設計・テスト設計の前提となる技術判断の記録 |

状態の凡例: **仮決定**＝このセッションでの壁打ちによる暫定決定（チームMTGで共有し、修正案があれば
フィードバックを受けて更新）／**決定**＝チーム内で既に確定済み／**改訂**＝後続ADRで置き換え済み。

**1行足すときのルール（ADR-0028以降）：「影響」欄に要件・仕様・設計・テスト設計の4つすべてについて
書く。** 関係ないものは「要件：なし」と明示する（空欄は「まだ確認していない」と区別がつかない）。
古いADRを上書きするときは、上書きされる側の「影響」欄にも1文追記する。手順とその理由は
`実装計画.md`「ADRを足したときの手順」にある。ADR-0001〜0027は2026-09-26に下流との
突き合わせ済み（欄の書式は当時のまま）。

| ID | 日付 | 起点 | 決めたこと | なぜ | 捨てた案 | 影響 | 撤退ライン | 状態 |
|---|---|---|---|---|---|---|---|---|
| ADR-0001 | 2026-09-24 | 壁打ち | ROIはA案（金額を出す）採用。旧試算（ROI3.8倍等）は取り下げ、前提を洗い直し再計算する | 提案書とチーム資料でROI提示方針が正反対だったため、方向性を先に固める必要があった | B案（金額を出さず、ダッシュボードをMust化して測る仕組みで示す） | 要求.md「成功の定義」／要件.md F-09（当時はShouldのまま据え置き。**その後ADR-0017でF-09＝独立ダッシュボードはWon'tになり、要件.mdから削除済み**） | 再計算した数値が説明に耐えなければB案に戻す | 仮決定（数値は再計算中） |
| ADR-0002 | 2026-09-21 | 壁打ち | BM25とベクトル検索を両方実行し統合するハイブリッド検索を採用 | 語彙が違う文書は意味検索が必然的に必要。固有名詞はBM25が強く両方要る | キーワード検索のみ（言い換え辞書）／意味検索のみ／LLMで言い換えて検索 | ADR-0003（統合方式）・ADR-0005（埋め込みモデル）が必要に。検索方式切替ボタンが必須に | 未設定（次回MTGで設定） | 仮決定 |
| ADR-0003 | 2026-09-21 | 壁打ち | RRF（k=60）で統合。各方式上位50件取得→統合後上位10件を返す | BM25とベクトルのスコアは尺度が違い直接比較できない。RRFは順位のみ使うため正規化不要、k=60は先行研究のデファクト値 | min-max正規化して合算／意味検索結果をBM25基準で並替え／Cross-Encoderで再採点 | RRFスコア（上位でも0.03程度）はそのまま％表示できない→ADR-0010で別途決定 | 未設定（次回MTGで設定） | 仮決定 |
| ADR-0004 | 2026-09-21 | 壁打ち | 専用ベクトルDBを導入せず、埋め込みをSQLiteのembeddingsテーブルにBLOB保存し検索時に内積計算 | 1,000件×384次元は通常PCで数ミリ秒。既存SQLite資産を使え運用がシンプル。Streamlit Cloudに追加ソフト不要 | FAISS／Chroma・Qdrant／pgvector | 文書数が数万件規模になったら再検討（本番フェーズの課題） | 文書数が数万件規模になった場合に専用DBを再検討 | 仮決定 |
| ADR-0005 | 2026-09-21 | 壁打ち | `intfloat/multilingual-e5-small`を第一候補、`cl-nagoya/ruri-small`と実データで比較し最終選定 | どちらもCPU動作・無課金。小型モデルで1,000件のバッチ変換が現実的な時間で終わる | OpenAI text-embedding-3等の外部API／multilingual-e5-large | `query:`/`passage:`プレフィックス付与をテストで担保。`normalize_embeddings=True`必須 | 実データでの検索品質評価がe5で不十分ならruriに切替 | 仮決定 |
| ADR-0006 | 2026-09-21 | 壁打ち | ニーズ⇔シーズを双方向検索。同じ側と反対側を別々に検索・統合する（`cross_search`のロジック）。**画面表示方法（当初は2カラム分離）はADR-0017で改訂** | 片方向だと研究所側の利用動機が消える。混ぜると同じ側が上位を独占し反対側（本命の価値）が埋もれる | 反対側だけを検索対象にする（旧サンプルコードの実装） | `cross_search`実装変更。往復両方向の実例確保がデータ準備の完了条件に追加 | 未設定（次回MTGで設定） | 仮決定（表示部分はADR-0017、同じ側検索の扱いはADR-0024で確定。**検索方向の切替はADR-0028で撤回し、タブ①はニーズ→技術の片方向に固定**） |
| ADR-0007 | 2026-09-21 | 壁打ち | 「過去の使われ方」を「技術→商品→訴求」ではなく「同じ悩みに対する既存の訴求・解決策」として実現する | 旧定義は社内企画書DBが必要で公開データからは実現不能。新定義はADR-0006の同じ側検索の副産物で実現可能 | 対応表を手作業で作る／機能自体を諦める | pptx Slide14の文言修正が必要。効果測定指標は「同じ側カードのクリック回数」。**実現手段はADR-0029で確定（同じ側検索＋`tech_usage`・`evaluations`）** | 未設定（次回MTGで設定） | 仮決定（実現手段はADR-0029で確定） |
| ADR-0008 | 2026-09-21 | 壁打ち | 「ヒットなし」は反対側の結果に限定して判定。BM25で0件かつベクトル類似度が基準値未満（暫定0.80）の両方 | 同じ側は必ず単語一致し0件にならない。反対側限定で「社内に技術がない」状態を正確に検出できる | 同じ側含め全体で判定／BM25のみで判定 | `search_logs.hit_count`は反対側のヒット数を記録。基準値はデータ投入後に調整 | 基準値調整で判定が安定しなければロジック見直し | 仮決定 |
| ADR-0009 | 2026-09-21 | 壁打ち | ログイン・権限管理は実装せず、立場切替ボタン（R&D／マーケ）をセッション単位で保持し検索方向と`user_dept`に使う | 検索方向決定とログ記録という2要件を1つのUI要素で満たせる。権限管理を対象外にした理由を明示しやすい | 本格的なログイン機能実装 | 「部門を越えて情報を見せていいのか」への回答は「本番フェーズで対応」と明記。**2役のうち検索方向の決定はADR-0028で撤回し、所属部署の選択（`user_dept`）のみ残す** | 未設定（Must仕様として確定） | 仮決定（検索方向の切替はADR-0028で撤回、ボタン自体もADR-0030で削除） |
| ADR-0010 | 2026-09-21 | 壁打ち | 表示する数値は意味検索側のコサイン類似度を％変換。表記は「一致度」でなく「関連度」。**並び順をRRFで決める部分はADR-0023で改訂** | RRFスコアは順位統合用で近さの指標ではない。「一致度」は判定している印象を与えAI非判定方針と矛盾する | 数値を表示しない／RRFスコアを正規化して表示 | pptx Slide13の表記修正。変換式はデータ投入後に決定 | 未設定（次回MTGで設定） | 仮決定（並び順の部分はADR-0023が上書き） |
| ADR-0011 | 2026-09-21 | 壁打ち | Janomeで単語分割し品詞フィルタ（名詞・動詞・形容詞のみ）を適用した`unicode61`を使用。trigramは不採用 | trigramは意味のない3文字断片で偶然ヒットし「0件になる」というデモの前提が崩れるリスクがある | trigram方式／MeCab+IPAdic | デモ用検索文でBM25が実際に0件になることをテストで確認必須 | Janome導入がデプロイで問題になればtrigram＋ストップワードで代替 | 仮決定 |
| ADR-0012 | 2026-09-26 | 壁打ち（特許庁API・INPIT開放特許DB調査を経て） | シーズ側は**INPIT開放特許情報データベースで化粧品関連の候補特許を検索・特定**し、少数（デモに必要な件数のみ、数十件規模）を**公式な特許公報（J-PlatPat）から人力で個別に転記**する。INPIT自体の技術概要等の文章はそのまま複製しない。J-STAGE・企業研究開発ページ・OpenAlexは不採用 | J-STAGE APIは規約でキャッシュ24時間制限があり本文保存不可。企業ページは語彙がFAQに近くBM25でも引けてしまう。INPIT開放特許DBは「自社で使っていない技術を他社に」という提案テーマと合致し、技術概要等の自然文フィールドも充実しているが、その文章自体はINPIT/登録者の著作物で複製・頒布が規約で禁止されているため、候補探しの検索ツールとしてのみ使う | J-STAGEを主収集源にする／企業の研究開発ページを主収集源にする／OpenAlexを採用する／INPITのCSV内容をそのまま複製する | 要件.md「実装時に確定するパラメータ」に収集手順を明記。特許庁の特許情報取得API（経過情報中心・キーワード検索不可）は不採用と確認済み | J-PlatPatが自動収集を検出・規約変更した場合、手動転記も含め収集方針を再検討 | 仮決定（件数・対象特許の最終選定はデータ準備時に確定） |
| ADR-0013 | 2026-09-24 | TA指摘→壁打ち | 検索結果の「わかりにくさ」対策はA（一言要約）のみ対応（ADR-0016）。B（抜粋表示）・C（担当部署表示）は対応しない | 一言要約で専門用語の壁という核心には対応済み。B・Cは3週間の制約で優先度を取らない | B＋C採用／D（用語集） | 要件.md「やらないこと」にB・Cを追加 | 対応しないと決定済み | 決定 |
| ADR-0014 | 2026-09-24 | TA指摘→壁打ち | 自動化は(a)将来の展開構想に明記するのみ。企画書作成・実験計画等の自動化は実装しない | 3週間・3名の制約で検索精度とデモ成立に集中すべき。ADR-0013でBを見送ったことと整合 | (b)過去検討からの論点出しを部分実装／完全な自動化実装 | 要件.md「やらないこと」に追加。パート5（展開構想）に項目追加 | 対応しないと決定済み | 決定 |
| ADR-0015 | 2026-09-21 | 壁打ち | `.db`はGit管理外。schema.sql・クローラー・投入スクリプトをコミットし、デモ用データは`fixtures/`に固定してコミット。起動時に自動組み立て | `.db`はバイナリで差分管理・マージ不可。クロール結果は変動しデモ再現性はスナップショット固定でのみ担保できる | Supabase等クラウド共有DB／`.db`を直接コミット／Git LFS | `fixtures/`の内容選定作業、テストはfixtures固定データに対して実施。**データ形式はADR-0025でJSON→seeds.sql（SQLite正本）に改訂** | 未設定（この方針で確定） | 仮決定（データ形式の部分はADR-0025が上書き） |
| ADR-0016 | 2026-09-24 | 要件定義中の壁打ち（ユーザー要望） | シーズ側技術文書に「平たく言うと」一言要約を併記。登録時1回だけ生成しDB保存、原文併記＋AI要約である旨を明記 | 登録時1回生成のためコスト・速度への影響が実質ゼロでADR-0005と非衝突。専門用語の壁という最小範囲に絞り実装コストを抑制 | 検索結果セット全体をRAGで動的要約（ADR-0005と衝突するため見送り、要件.mdF-15として残す） | 要件.mdにF-16（Must）追加 | 対応する決定済み | 決定 |
| ADR-0017 | 2026-09-26 | 壁打ち（§7-5画面設計の読み込みにより） | 画面構成を§7-5に統一。**タブ①検索**（企画案を入力→一致度順の統一カードリストに一致度・一言要約・一致理由・権利状況・使用実績を集約）と**タブ②技術データベース**（一覧・絞り込み・詳細）の2画面のみとする。ADR-0006の「2カラム分離表示」・独立ダッシュボード画面（旧F-09）は採用しない | 提案書§7-5がADR-0006策定後により詳細化され、画面②③を検索結果＋技術資産ブラウズの2タブ構成に整理し直していた。2カラム分離のままだと情報量に対しUIが複雑化し3週間の実装量を超えるおそれがある | ADR-0006どおりの2カラム分離表示を維持／独立したダッシュボード画面を実装 | 要件.md F-03を改訂しF-09をWon'tへ。ゼロ件クエリの可視化は技術データベースタブの絞り込み「まだ社内に無い技術」として実現 | 実装してみて1枚のカードに情報が収まらない場合、同じ側/反対側の分離表示に戻す | 仮決定 |
| ADR-0018 | 2026-09-26 | 設計.md策定中の壁打ち | F-18（一致理由ハイライト）の語句単位ベクトルは専用テーブルに事前保存せず、検索結果上位の文書に対してのみ都度計算する | 事前保存には新テーブル（phrase_embeddings）と文書登録時のチャンク分割ロジック変更が必要で3週間の実装コストが増す。都度計算は対象が上位10件程度なので計算コストが小さい | phrase_embeddingsテーブルで事前計算・保存 | `search/highlight.py`は検索結果に対してのみ計算。仕様.md「入力と出力」F-18行を更新 | 上位10件×文書長で応答速度が数秒を超える場合、対象を上位3件に絞るか事前計算に戻す | 決定 |
| ADR-0019 | 2026-09-26 | 設計.md策定中の壁打ち | 訴求の競合チェック・差別化の空き地判定（F-20）は今回のPoCでは実装しない（Won't） | 他社商品ページの収集経路（自動クロールの規約確認／手動転記の運用）が未整備で、シーズ収集（INPIT＋特許）と並行してもう一つの収集経路を3週間で整備する余力がない | 少数を人力で手動登録／自動クロールで対応 | 要件.mdのF-20を「やらないこと」へ移動。仕様.md・設計.mdの該当記述（`doc_type='product'`、`company`列を含む）を削除 | 余力が明確になれば少数手動登録で復活検討。本番フェーズの拡張案として展開構想に記録 | 決定 |
| ADR-0020 | 2026-09-26 | 壁打ち（ユーザー質問：手動転記した特許にembeddingをどう入れるか） | 一言要約（`summary_plain`）は`fixtures/`準備時に1回だけオフラインで生成し、テキストをfixturesのJSONに書き込んでおく（起動時にAPIを呼ばない）。埋め込みベクトルはローカル無料計算のため、DB自動構築のたび（＝Streamlit Cloud起動のたび）に`embed_batch.py`で再計算してよい | `summary_plain`は外部の課金APIを呼ぶため、ADR-0015の「`.db`は起動のたびに作り直す」と組み合わせると起動のたびに課金・生成結果の揺れが発生する。埋め込みはローカル無料計算のため毎回再計算しても問題ない | summary_plainも起動のたびに生成する／embeddingsも事前計算済みBLOBとしてfixturesに含める（JSONでバイナリを扱う実装が煩雑） | `fixtures/seed_patents.json`に`summary_plain`列を追加。`build_db.py`の処理順は「documents投入→embed_batch.py（毎回）」。`summarize.py`は開発時に一度だけ手動実行するスクリプトと位置づける | 起動時間が長すぎる場合、embeddingsも事前計算してfixturesに含める方式に変更 | 決定 |
| ADR-0021 | 2026-09-26 | 壁打ち（ユーザー指摘：要約だけではニーズに対応しているか返せない） | シーズ文書は要約だけでなく**【背景技術】と【発明が解決しようとする課題】も転記**し、`documents`に`background`/`problem`列として別々に保持する。埋め込みは要約・課題・背景を**フィールドごとに別ベクトル化**し、検索時は最も近いフィールドのスコアをその文書のスコアとする（`embeddings`を`(doc_id, field)`の複合キーへ変更） | 【要約】は「解決手段」の記述で技術同士の類似しか拾えない。【課題】は「問題」の記述で生活者ニーズとの意味的距離が近く、ニーズ→シーズのマッチングに最も効く。フィールド単位で埋め込めばe5の512トークン上限による切り捨ても起きない。課題文は「何を解決する技術か」を発明者自身の言葉で示すためカード表示としても価値が高い | タイトル＋課題＋要約を連結して1ベクトル（512トークン超過で末尾が切り捨てられる）／チャンク分割（手作業の転記では準備が煩雑） | 仕様.mdの`documents`に`background`/`problem`列を追加、`embeddings`を複合キーへ。転記項目に2項目追加。設計.mdの`vector.py`にdoc_id単位での集約処理を追加 | 課題文にニーズ寄りの語が多くBM25が0件にならない場合、`check_gap.py`の結果を見てデモ用クエリを選び直す（BM25索引の対象フィールドを絞ることも検討） | 決定 |
| ADR-0022 | 2026-09-26 | テスト設計.md策定中（受入基準がテスト不能と判明） | 評価データ・見送り記録・未評価領域を格納する`evaluations`テーブルを新設し、`tech_usage`と同じくデモ用の架空データを数件手で登録して画面に「サンプルデータ」と明示する | 要求の受入基準「過去に検討して断念した記録が表示される」とF-05のエビデンス表示に対応する置き場が仕様のデータ定義になく、テスト不能だった。提案書§4-2は「やって駄目だったかも出る」を★時間短縮の本体と位置づけており、落とすと提案の骨格が一本抜ける。特許データからは社内の断念経緯は取得できないため架空データで代替する | `documents`にdoc_typeを増やして混ぜる（検索対象に混ざり除外処理が必要）／PoCのスコープから外す（提案の中核メッセージが実演できなくなる） | 仕様.mdのデータに`evaluations`テーブルを追加。設計.mdの`repositories.py`・`fixtures/`に反映。テスト設計のA-03・S-05の保留を解除 | 架空データであることが伝わらず「本物のデータか」と問われて崩れる場合、画面表示の注記を強める | 決定 |

| ADR-0023 | 2026-09-26 | デモ実装中に判明（表示％が降順に並ばない） | **表示順は関連度（コサイン類似度）の降順とする。** RRFは「どの文書を結果に含めるか」の決定にのみ使い、並び順には使わない。あわせて候補集合に入った文書には全件分の類似度を計算して付与し、BM25だけでヒットした文書も同じ土俵で並べる | ADR-0010の「並び順はRRF、表示する数値はコサイン類似度」をそのまま実装すると、BM25にもヒットした文書がRRFで上位に来つつ表示％は低い状態が起き（3位68%、4位88%）、発表で「なぜ68%が88%より上なのか」と必ず突っ込まれる。提案書が一貫して避けてきた種類の穴 | RRF順のまま％の前後を説明する／％を表示せず順位だけ出す（pptx案から後退する） | ADR-0010の「並び順はRRFで決める」部分を上書き。`search/cross_search.py`で候補選定後に関連度降順へ並べ替え。`vector.topk_by_doc`を`best_by_doc`＋`rank`に分割 | BM25でしか見つからない固有名詞ヒットが埋もれるようなら、固有名詞一致を別枠で上位に固定表示する | 決定 |

| ADR-0024 | 2026-09-26 | べーさんの指摘（0件判定の対象はシーズ側か、ニーズ側は検索されてよいか） | **同じ側（ニーズ側）の検索を復活させる。** 反対側（技術）を主役のカードリストとして表示し、その下に「似た悩み・既存の訴求」を折りたたみで最大3件、別枠で表示する。「ヒットなし」判定は引き続き**反対側に対してのみ**行う（ADR-0008は変更なし） | ADR-0006は「同じ側の結果も返す」と決めていたが、ADR-0017の画面変更（2カラム→統一カードリスト）のとき表示方法だけでなく同じ側の検索そのものが実装から落ちていた。ADR-0007（「過去の使われ方」＝同じ悩みへの既存の訴求）は同じ側検索の副産物で実現する前提だったため、落としたままだと実現手段がなくなる | 同じ側を復活させない（ADR-0006・0007の改訂が必要）／技術データベースの詳細画面内だけで使う | `cross_search`が反対側と同じ側を別々に検索し`same_side_hits`で返す。タブ①に折りたたみセクションを追加。1つの順位表には混ぜない（同じ側が上位を独占して主役が埋もれるため） | 同じ側の表示が主役を食う・画面が煩雑になる場合、技術データベースの詳細画面のみへ移す | 決定 |

| ADR-0025 | 2026-09-26 | ユーザー要望（特許情報はJSONでなくDBで管理したい） | **手入力データの編集先を`knowledge.db`（SQLite）本体にする。** コミット前に`tools/export_seeds.py`を1回実行し、`fixtures/seeds.sql`（テキスト）へ書き出したものをGitに乗せる。`.db`は引き続きGit管理外で、起動時に`schema.sql`＋`seeds.sql`から組み立てる。JSONのfixturesは廃止 | 転記30件をJSONで手書きするのは非現実的で、DBツールで表として編集できるほうが速く誤りも減る。またJSON方式には「`.db`が既にあるとJSONを直しても反映されない」という罠があり、転記作業中に確実に踏む。編集先をDB1つにすれば可動部が減りこの事故が消える。テキストで書き出すのは、ぐんさん・べーがPRでデータの中身をレビューできるようにするため（`.db`のままだとバイナリで差分が読めず、レビューが形骸化する） | `.db`をそのままコミットする（操作は増えないがPRレビュー不可）／JSONのまま続ける（手書きが重く、反映されない罠が残る）／入力用のStreamlit画面を作る（転記は最も楽だが仕様外の機能でスコープが増える） | ADR-0015の「デモ用データはfixturesにJSONで固定」およびADR-0020のJSON前提の記述を改訂。`build_db.py`はseeds.sqlを読む方式へ。`export_seeds.py`を追加。embeddings・search_logsは書き出し対象外（前者は再計算、後者は実行時ログ） | 書き出し忘れでコミット内容が古くなる事故が続くなら、pre-commitフックで自動実行に切り替える | 決定 |

| ADR-0026 | 2026-09-26 | `check_gap.py`の実測（長い企画案だとBM25が0件にならない。ADR-0021の撤退ラインで想定していた事象） | **デモは2つの入力を順に見せる。** ①生活者の声をそのまま（30字程度の短文）→ キーワード0件・ハイブリッド10件。②企画案の文章（70字程度）→ キーワードは3件返すが1位が無関係な技術で正解は2位、ハイブリッドで正解が1位（96%）。N-06／U-04／S-22の「BM25が0件」は**①に対してのみ**課す | 実測すると長文では「整髪」「重い」が特許の**背景技術**と一致してBM25が3件返る。これはダミーデータの作り込み不足ではなく実在の語彙重複で、企画案が製品カテゴリ名（整髪料）を書く以上どんなデータでも起きる。一方、その3件はキーワードだけでは並びが壊れており、「重い」1語で引っかかった摩擦係数の特許が1位、正解は2位に埋もれる。「0件になる」と「埋もれる」の2つを見せたほうが語彙の断絶の説明として強く、かつ実測どおりで嘘がない | BM25索引の対象を要約(`body`)だけに絞り長文でも0件にする（ADR-0021の撤退ラインだが、キーワード検索をわざと弱めた形になり発表で必ず突かれる）／長文だけでデモ（0件の絵が撮れない）／短文だけでデモ（提案書§7-5の「企画案をそのまま入力」と噛み合わない） | テスト設計のU-04・S-22・A-01の入力を「デモ用ニーズ文（短文）」と明記し、A-01に長文の観点を追加。実装計画2-13は短文・長文の両方を出す。実特許15件の投入後に再実測する | 実特許に差し替えた結果、長文でもキーワードが正解を1位に出すようなら②を落とし①のみでデモする | 決定（9/26・PM承認） |

| ADR-0027 | 2026-09-26 | 5文書の突き合わせで発見（仕様.mdのFTS定義が実装と違っていた） | **`documents_fts`は`title`/`body`/`problem`/`background`の4列をcontentless構成（`content=''`）で索引し、Janomeで分かち書き済みのテキストを`build_db.py`が明示的に投入する。** external content方式＋トリガ同期は使わない。ベクトル側は`title`を除く3フィールド（ADR-0021）なので、**BM25とベクトルで対象フィールドが意図的に違う** | 仕様.mdは`fts5(title, body, content='documents')`＝2列・external content・トリガ同期と書いていたが、実装は4列・contentless。**この差はADR-0026の根拠そのもの**で、長文の企画案でBM25が0件にならないのは`background`の「整髪料」を索引しているため。仕様どおり2列ならADR-0026の②が成立しない。課題・背景を索引するのはニーズ語に最も近い記述がそこにあるから（ADR-0021と同じ理由）。contentlessにするのは、索引に入れるのがJanomeで分かち書きしたテキストで`documents`の原文とは別物であり、external contentの自動同期では分かち書きを挟めないため。`title`をBM25では索引しベクトルでは埋め込まないのは、語の完全一致は有用だがベクトルでは短文の類似度が一様に高く出て最大値方式を壊すから | 仕様どおり2列＋external content＋トリガに実装を戻す（課題・背景の語がBM25で引けなくなり、ADR-0026の②が成立しない）／4列のままexternal contentにする（分かち書きを挟む場所がない） | 仕様.mdの`CREATE VIRTUAL TABLE`と「エラー時の挙動」、テスト設計I-05を「トリガ同期」から「build_dbが明示投入」へ修正。テスト設計にU-22（4列すべてが索引される）を追加 | 索引の張り直し忘れによる事故が起きるなら、`documents`へのINSERT/UPDATEをリポジトリ層に集約し索引更新を同時に行う | 決定 |

| ADR-0028 | 2026-09-26 | ユーザー（PM）の指摘：R&D立場で生活者の声を返しても、それは生活者全体の意見ではないので根拠にならない | **タブ①の立場切替から検索方向の切替を外す。** 検索はニーズ→技術の片方向に固定し、ボタンは`search_logs.user_dept`を入れるための**所属部署の選択**として残す。研究員の用途探索はタブ②の詳細画面が担う（ADR-0029） | seed→need検索が返すのはニーズ文書（ダミー＋実在FAQ少数）で、nが小さく母集団を代表しない。「関連度96%」を市場ニーズの根拠として見せると、**件数を増やしても正当化できない主張**になる。PoCで代表性のあるVOCは集められないため、データを足して解決する種類の問題ではない。一方タブ②の詳細画面には既に同等以上の機能（`evaluations`・`tech_usage`・関連技術）があり、立場切替は二重になっていた。実装期間7日に対してMustを1つ軽くできる効果もある | 現状維持（代表性の問題が残り発表で突かれる）／ボタンごと削除（F-04の`user_dept`が欠けROI測定の材料が減る）／R&D立場のときseed→seed＋実績を返す（タブ②と機能が重複しUI実装が増える） | **要件**：F-07を「検索方向と所属部門を切り替える」から「所属部門を選んでログに記録する」へ縮小（Mustのまま）。**仕様**：タブ①から検索方向の切替を削除、入力と出力F-02の`query_side`を'need'固定へ、F-07の出力をログ属性のみに。**設計**：`tab_search.py`が`query_side`を固定。`cross_search`の双方向インターフェースは残す（タブ②が使う）。**テスト設計**：A-04・S-07を「立場を選ぶと`search_logs.user_dept`に反映される」へ差し替え | 研究員側の利用動機が説明できないと指摘されたら、タブ②を「研究員の入口」として明示する導線を足す（検索方向の復活はしない） | 決定（9/26・PM承認。**残した所属部署ボタンもADR-0030で削除**） |

| ADR-0029 | 2026-09-26 | 同上（ADR-0028と同じ壁打ち）。「技術案を入れて活用シーンを探りたいが、何を根拠に判断できるか分からない」 | タブ②詳細の「この技術を求めている声」を**「似た課題を解く技術と、その使われ方」**に差し替える。同じ側（seed→seed）で近い技術を出し、各技術の`tech_usage`（どの商品にどんな訴求で使われたか）と`evaluations`（評価済み／見送り／未評価）を併記する。生活者の声は消さず、**「関連する収集済みの声（出典つき。母集団を代表しません）」**として下に併記する | 「この技術は何に使えるか」の根拠を、**意見のサンプルから記録に変える**。「同じ課題を解く技術が実際にこの用途で商品になった」は事実であり、AIが可否を判定しない方針と一致する。見送り・未評価も同時に出るため「行けるかも」だけでなく「そこは踏まれて駄目だった」「そこはまだ誰も見ていない」まで渡せる。必要なデータは`tech_usage`・`evaluations`で既にスキーマにある（ADR-0007・0022） | 声をそのまま残し注記だけ足す（10分の発表では注記が効かず代表性の指摘に耐えない）／セクションごと削除（ADR-0007「過去の使われ方」の実現手段が無くなる） | **要件**：F-08の受入基準に「似た課題を解く技術の実績・評価も併せて表示」を追加し、ADR-0007の実現手段が同じ側検索＋`tech_usage`であることを明記。**仕様**：タブ②詳細の該当項目を差し替え。**設計**：`tab_techdb.py`の当該処理を同じ側検索＋`get_tech_usage`/`get_evaluations`へ変更。**テスト設計**：S-08の期待結果を更新、I-11を追加 | 技術同士の類似度が低く（実測で51%・41%・35%）並びが説得力を欠く場合、関連度の表示をやめ「近い課題を解く技術」の一覧として件数のみ示す | 決定（9/26・PM承認） |

| ADR-0030 | 2026-09-26 | ユーザー（PM）の判断：立場はマーケのみでよい、R&Dは削除する | **R&Dを利用者から外し、所属部署ボタンごと画面から削除する。** 利用者はマーケティング／商品企画の1種類だけ。**F-07は「やらないこと」へ（旧F-07）。** `search_logs.user_dept`の列は残し、`'MK'`固定で記録する。タブ②詳細の「似た課題を解く技術と、その使われ方」（ADR-0029）は**削除せず残す**が、位置づけを「研究員の用途探索」から**「企画者が技術の使いどころと差別化の余地を判断する材料」**に読み替える | ADR-0028でボタンの役目は`user_dept`の記録だけになっていたが、その`user_dept`の唯一の消費先だった部門またぎ検索率の可視化は旧F-09（ADR-0017でWon't）で既に無い。**選ぶ意味のない選択肢が画面に残っている状態**で、デモでも「なぜこれを選ぶのか」を説明できない。要求.mdは元から利用者をマーケ1人に絞っており（約80名・研究開発と兼務）、R&Dを外すほうが上流と一致する。Mustがもう1つ減り、実装期間7日に効く | 部署ボタンを残し`user_dept`を記録し続ける（選ぶ意味を説明できない）／`user_dept`の列ごと削除する（本番で部門が増えたとき復活が要る。列1つ残すコストは小さい） | **要件**：F-07を「やらないこと」へ移し「旧F-07」と注記。F-04の受入基準を「`user_dept`は`'MK'`固定で記録」に修正。**仕様**：タブ①から所属部署ボタンを削除、入力と出力からF-07の行を削除、`search_logs.user_dept`のコメントを`'MK'`固定に。**設計**：`app.py`からボタンを削除、処理の流れの1番を削除。タブ②「まだ社内に無い技術」の一覧から「部署」列を外す（全行同じ値になるため）。**テスト設計**：A-04・S-07・I-12を削除し、S-04の期待結果に「`user_dept`が`'MK'`で入る」を追加 | 本番フェーズで研究員の利用を入れるときは、ADR-0028・0029の議論（声は母集団を代表しない／根拠は記録に置く）から再開する | 決定（9/26・PM承認） |

## 次回MTGの論点

| # | 論点 | 関連ADR | 決めること |
|---|---|---|---|
| 1 | 仮決定14本（ADR-0001〜0012・0015・0017）を採用してよいか | 各該当ADR | チームでの承認（修正案があればフィードバック） |
| 2 | ADR-0001：ROIの数値を誰がいつまでに再計算するか | 0001 | 前提の置き方・担当・期限 |
| 3 | 特許の転記作業の分担（検索キーワード・件数） | 0012 | 10/1着手・10/2完了で間に合うか |
| 4 | サッポロのアプリとの関係 | 0014 | たっきーへの確認担当 |
| 5 | デモ台本の2本立て（短文＝キーワード0件／長文＝正解が2位に埋もれる）の共有 | 0026 | 決定済み。実特許15件の投入後に再実測する担当（べー・10/1の2-13）を確認 |

## 未決事項の一覧

| # | 内容 | 期限 |
|---|---|---|
| 1 | ADR-0001：ROIの具体的な数値の再計算（方向性はA案で決定済み） | 次回MTGまで |
| 2 | タブ①検索カードの表示尺（一致度・要約・一致理由・権利状況・使用実績を1枚にどう収めるか、ADR-0017） | 設計.md策定時 |
| 3 | 「ヒットなし」判定の基準値（方針決定済み・暫定0.80から実データで調整。ADR-0008） | データ投入後 |
| 4 | 関連度スコアの変換式（方針決定済み・実データで調整。ADR-0010） | データ投入後 |
| 5 | 埋め込みモデルの最終選定（方針決定済み・e5を第一候補、時間があればruriと比較。ADR-0005） | データ投入後 |
| 6 | INPIT開放特許DBで検索する化粧品関連キーワードの選定、転記する特許の件数・担当（ADR-0012） | データ準備時・最優先 |
| 7 | pptx Slide 13「一致度」→「関連度」、Slide 14「過去の使われ方」の文言修正 | 発表準備開始まで |
| 8 | サッポロのアプリの機能範囲確認（担当：未定） | 次回MTGまで |
