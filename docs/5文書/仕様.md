# 仕様 — 知の越境検索（PROJECT ZERO / W6）

前提: Streamlit単一プロセスのPoC（Streamlit Community Cloudにデプロイ）。フロント・検索ロジック・
ログ記録を同一プロセスで実行し、Next.js＋FastAPIのような層分離は行わない。

## 全体像
マーケ担当者／研究員がタブ①検索に企画案の文章（検索語ではなく文章）を入力すると、Janome＋品詞
フィルタでトークナイズしたBM25検索と、`multilingual-e5-small`によるベクトル検索を実行し、RRF
（k=60、各方式上位50件→統合後上位10件）で統合した結果を、関連度順の統一カードリストとして返す
（ADR-0017）。各カードには一言要約・一致理由・権利状況・使用実績を表示する。タブ②技術データベース
では技術一覧を絞り込み・詳細閲覧できる。データは事前に投入されたSQLite（`documents` /
`embeddings` / `tech_usage` / `patents` / `search_logs`）を参照し、検索時に新たな生成AI呼び出し
は行わない（要約は登録時に1回のみ生成・保存）。立場切替ボタン（R&D／マーケ）でクエリの方向と
ログの所属部門を切り替える。

## 画面・操作

### タブ①検索（デモの主役）
- 立場切替ボタン（R&D／マーケ）。選択値はセッション単位で保持し、検索方向と`search_logs.user_dept`
  に使う（ADR-0009）
- 検索方式トグル（キーワードのみ／意味のみ／両方併用）。**デモ専用の控えめな帯**に置き、通常画面
  には出さない（F-06・F-10）
- 検索窓：企画案の文章をそのまま入力する（例：「夕方になると前髪がベタついて束になる、という
  20〜30代男性の不満に応えたい…」）。短い単語より文章の方がベクトル検索の精度が出る
- 検索結果カードリスト（関連度順、F-03）。カード1件の表示項目：
  - 技術名／文書タイトル、`doc_type`ラベル（シーズ／ニーズ）
  - 関連度（%表示。相対値に変換したもの。素のコサイン類似度をそのまま出さない）
  - **この技術が解決しようとしている課題**（`problem`。発明者自身の言葉なので、企画側が「自分の
    ニーズに効くか」を判断する主材料になる。ADR-0021）
  - 一言要約（シーズ側のみ。F-16、「※AIによる要約です」注記付き）
  - 一致理由ハイライト（語句単位。F-18）
  - 権利状況バッジ（登録済／出願中／未出願。F-19）
  - 使用実績サマリ（採用実績の有無。F-08への導線）
  - エビデンスへのワンクリックリンク（F-05）
- **「ヒットなし」判定（ADR-0008）でも画面は空にしない。** ベクトル検索は仕組み上つねに上位N件を
  返すため、反対側でBM25=0件かつベクトル類似度が基準値未満の場合も、関連度%が低い「応用できるか
  もしれない候補」としてカードは表示され続ける（§4-2「候補はゼロにしない」）。この場合はカード
  リストの上に言い換え候補を案内する（F-21：「十分一致する技術は見つかりませんでした。もしかして：
  べたつく → 皮脂、粘性 も検索しますか？」）。裏側では`search_logs.hit_count=0`として記録され、
  その企画案がタブ②「まだ社内に無い技術」に個別の1件として追加される
- カードをクリックするとタブ②の詳細画面に遷移する

### タブ②技術データベース
- 技術一覧。絞り込み条件：「未使用の技術」「特許 登録済／出願中」「まだ社内に無い技術」（F-17）
- 「まだ社内に無い技術」は**頻度ランキングにしない**。個別の企画案を並べ、検索日・検索した部署・
  その時の最高関連度を表示する（各企画案は文面が毎回異なるため集計は実態に合わない）
- 詳細画面：
  - エビデンス（原文へのリンク・`evaluations`の評価済み／見送り／未評価・`patents`の関連特許）を
    同じ場所に並べる（F-05、ADR-0022）。「やって駄目だったか」「まだ評価されていない」ことも
    判断材料として明示する
  - 自社での使われ方（`tech_usage`の採用実績一覧。F-08）
  - この技術を求めている声（反対方向のクロス検索結果。使われ方B、研究員向け・前面には出さない）
  - ※訴求の競合チェック・空き地判定は今回のPoCでは対応しない（ADR-0019）

## 入力と出力

| 要件ID | 入力 | 処理 | 出力 |
| --- | --- | --- | --- |
| F-01 | 手動転記した特許（要約・背景技術・課題）、ダミーのニーズ文 | `doc_type`/`source`/`content_hash`を付与しDBへ格納。`body`/`problem`/`background`/`title`をフィールドごとに埋め込み（ADR-0021）。`summary_plain`はfixtures準備時に生成済みのテキストを投入（ADR-0020） | `documents`の行、`embeddings`の行（1文書につきフィールド数分） |
| F-02 | 企画案の文章、`query_side`（'need'固定。ADR-0006の使われ方Aが基本） | `cross_search()`：BM25で対象`doc_type`から上位50件、ベクトル（`query: `プレフィックス付き）を全フィールドのベクトルと比較し**doc_idごとに最大類似度を採用**して上位50件。**RRF（k=60）はどの文書を結果に含めるかの決定にのみ使い、並び順には使わない**（ADR-0023） | 文書リスト（doc_id, 関連度, 最も効いたフィールド名, BM25/ベクトルどちらでヒットしたか） |
| F-03 | F-02の結果 | **関連度の降順に並べ替え**、カード表示用に整形（％変換、`summary_plain`・`patents`・`tech_usage`を結合）。候補集合に入った文書には全件分の類似度を計算して付与し、BM25だけでヒットした文書も同じ土俵で並べる（ADR-0023） | 関連度順カードリスト |
| F-04 | query, mode, user_dept, hit_count | `st.button`のブロック内でのみ`search_logs`へINSERT（Streamlitの再実行モデルに注意。ボタン外に書くと水増しされる） | `search_logs`の行1件 |
| F-05 | doc_id | `documents`から原文URL、`evaluations`から評価済み／見送り／未評価の記録、`patents`から関連特許を取得（ADR-0022） | エビデンス表示（原文リンク・評価データ・見送り記録・未評価領域） |
| F-06 | 検索方式選択（bm25／vector／hybrid） | `cross_search`内の統合ロジックを切替 | 結果件数・内容の変化（0件→3件のデモ） |
| F-07 | 立場選択（R&D／マーケ） | `query_side`と`user_dept`を`st.session_state`に設定 | 検索方向・ログ属性の切替 |
| F-08 | doc_id（seed側） | `tech_usage`を`seed_id`で検索 | 採用実績一覧（product, brand, claim, launched） |
| F-10 | 検索クエリ | bm25単独／vector単独／hybridをそれぞれ実行 | 3方式の結果を並べた比較表示 |
| F-11 | クロール対象URL一覧 | GitHub Actions夜間バッチで取得→`content_hash`比較→変更分のみ更新 | `documents`の差分更新 |
| F-12 | ローカルフォルダのパス | `markitdown`等で本文抽出→チャンク分割→登録（W2資産の分岐） | `documents`への新規登録（デモ用） |
| F-13 | 新規登録文書 | 反対側の既存文書との類似度が閾値超か判定 | マッチ一覧への追加通知 |
| F-14 | 検索結果 | bm25_hits／vec_hitsどちらに含まれるかを判定 | 「語一致」ラベル、または関連度%表示 |
| F-16 | seed文書の登録時 | 生成AI APIを1回だけ呼び出し要約生成 | `documents.summary_plain` |
| F-17 | 絞り込み条件 | `documents`/`patents`/`tech_usage`/`search_logs`を条件でフィルタ | 一覧・詳細画面 |
| F-18 | 検索結果上位の文書、クエリ文 | 双方を語句単位に分割し、その場でベクトル化してコサイン類似度で最近傍ペアを抽出（事前保存しない。ADR-0018） | ハイライト箇所（例：「べたつく」⇔「可塑化」） |
| F-19 | doc_id（seed側） | `patents`を`seed_id`で検索 | state（登録済／出願中／未出願）、番号、各日付、note |
| F-21 | ヒット件数 | 閾値未満なら類義語・近傍語から言い換え候補を生成 | 「もしかして：〇〇 も検索しますか？」の提示 |

## データ

```sql
-- 文書本体
CREATE TABLE documents (
  id            INTEGER PRIMARY KEY,
  doc_type      TEXT NOT NULL,   -- 'seed' | 'need'
  source        TEXT NOT NULL,   -- 'patent_manual' | 'dummy' | 'faq'
  title         TEXT,
  body          TEXT,            -- 【要約】＝解決手段の記述
  background    TEXT,            -- 【背景技術】（seed側のみ。ADR-0021）
  problem       TEXT,            -- 【発明が解決しようとする課題】＝問題の記述。ニーズとの照合に最も効く（ADR-0021）
  url           TEXT,
  pub_date      TEXT,
  category      TEXT,            -- ヘアスタイリング / スキンケア ...
  dept          TEXT,            -- 'R&D' | 'MK'（本番機能。PoCでは非表示）
  summary_plain TEXT,            -- 「平たく言うと」要約（seed側のみ、登録時1回生成。F-16）
  parent_id     INTEGER,         -- チャンク分割元の文書ID
  chunk_no      INTEGER,         -- 章・節単位の分割番号
  content_hash  TEXT,            -- 差分検知キー（SHA-256。F-11）
  fetched_at    TEXT
);
-- ※`company`列・`doc_type='product'`は訴求の競合チェック（旧F-20）用だったが、ADR-0019により
--   今回は対応しないため設けない。復活時にADRを切って追加する

-- 技術の使われ方（採用実績・転用実績。F-08）
CREATE TABLE tech_usage (
  id         INTEGER PRIMARY KEY,
  seed_id    INTEGER REFERENCES documents(id),
  product    TEXT,
  brand      TEXT,
  claim      TEXT,
  launched   TEXT,
  source_url TEXT
);
-- PoCではデモ用に数件を手で登録し、架空データと明示する

-- 評価・見送り記録（F-05のエビデンス。ADR-0022）
CREATE TABLE evaluations (
  id      INTEGER PRIMARY KEY,
  seed_id INTEGER REFERENCES documents(id),
  kind    TEXT,      -- 'evaluated'（評価済み）| 'dropped'（見送り）| 'unevaluated'（未評価領域）
  summary TEXT,      -- 「安定性の問題で見送り」等、一行で分かる内容
  detail  TEXT,      -- 評価データの要点（条件・数値など）
  dated   TEXT       -- 実施・判断した年月
);
-- PoCではデモ用に数件を手で登録し、画面に「サンプルデータ」と明示する（tech_usageと同じ扱い）
-- 「やって駄目だったか」が分かることが時間短縮の本体（提案書§4-2）。'unevaluated'は
-- 「データがない」ことも判断材料になるため、空欄で隠さず明示的に持つ

-- 権利状況（F-19）
CREATE TABLE patents (
  id         INTEGER PRIMARY KEY,
  seed_id    INTEGER REFERENCES documents(id),
  state      TEXT,      -- 'registered' | 'pending' | 'none'
  number     TEXT,
  filed      TEXT,
  registered TEXT,
  expires    TEXT,
  note       TEXT        -- 請求範囲の限定など人が確認すべき事項
);
-- state は一覧段階から表示する。請求範囲の判断は行わず、note に確認事項を書くに留める（ADR-0012）

-- 全文検索（FTS5、BM25内蔵）
CREATE VIRTUAL TABLE documents_fts USING fts5(
  title, body,
  content='documents', content_rowid='id',
  tokenize='unicode61'   -- Janomeで分かち書き。trigramは不採用（ADR-0011）
);
-- external content方式のためトリガでの同期が必須（DBに入っているのに検索に出ない不具合を防ぐ）

-- ベクトル
CREATE TABLE embeddings (
  doc_id INTEGER NOT NULL REFERENCES documents(id),
  field  TEXT NOT NULL,   -- 'body'（要約）| 'problem'（課題）| 'background'（背景技術）| 'title'
  vec    BLOB NOT NULL,   -- float32配列をnumpy.tobytes()で格納。normalize_embeddings=True（ADR-0004・0005）
  PRIMARY KEY (doc_id, field)
);
-- 1文書につきフィールドごとに別ベクトルを持つ（ADR-0021）。検索時は最も類似度の高いフィールドの
-- スコアをその文書のスコアとして採用し、doc_id単位に集約する。
-- 連結して1ベクトルにしないのは、e5の512トークン上限で末尾が切り捨てられるのを避けるため。

-- 検索ログ
CREATE TABLE search_logs (
  id               INTEGER PRIMARY KEY,
  ts               TEXT,
  query            TEXT,
  mode             TEXT,     -- 'cross' | 'normal'
  user_dept        TEXT,     -- 'R&D' | 'MK'
  hit_count        INTEGER,  -- 反対側のヒット数。0なら「社内に知見がない空白領域」（ADR-0008）
  clicked_doc_id   INTEGER,
  clicked_doc_type TEXT
);
```

**データソースの方針（ADR-0012）**
- シーズ側：INPIT開放特許情報データベースで化粧品関連の候補特許を検索・特定し、デモに必要な件数
  （数十件規模）だけ公式な特許公報（J-PlatPat）から人力で転記する。`source='patent_manual'`とし、
  INPIT側の技術概要等の文章はそのまま複製しない。転記する項目は以下のとおり（明細書全文は転記
  しない。原文参照は`url`のリンク先に委ねる）：
  - `documents`：`title`←発明の名称／`body`←【要約】の全文／`background`←【背景技術】／
    `problem`←**【発明が解決しようとする課題】**（ニーズとの照合に最も効くフィールド。ADR-0021）／
    `url`←当該ページURL／`pub_date`←公開日／`category`←自分で判断（ヘアスタイリング等）
  - 【発明の詳細な説明】の全文は転記しない。原文参照は`url`のリンク先に委ねる
  - `patents`：`state`←経過情報から判定／`number`←特許番号または出願番号／`filed`←出願日／
    `registered`←登録日（あれば）／`expires`←存続期間満了日（あれば）／`note`←任意
  - `tech_usage`（採用実績）・`evaluations`（評価・見送り記録）は特許本体とは別に、デモ用の
    架空データとして手で作文し、画面に「サンプルデータ」と明示する（ADR-0022）
- ニーズ側：基本はダミーデータを手動登録（`source='dummy'`）。デモで使う検索語1文だけは実在する
  FAQから引用し、出典URLを残す（`source='faq'`）
- 訴求の競合チェック（旧F-20）は今回のPoCでは対応しない（ADR-0019）。他社商品ページの収集は行わない
- 画面には「サンプルデータ」等、実データと判別できる表示を行う

**保存先の方針**：PoCは`documents`/`embeddings`/`search_logs`ともSQLite。`.db`はGit管理外で、
起動時に`schema.sql`＋`fixtures/seeds.sql`から自動組み立てる（ADR-0015・0025）。
**手入力データの編集先は`knowledge.db`本体**で、コミット前に`tools/export_seeds.py`を1回実行して
`seeds.sql`へ書き出す（ADR-0025）。Streamlit Community Cloud
はアプリ再起動でファイルがリポジトリの状態に戻るため、**`search_logs`はアプリ起動中のみ有効**（発表
当日のライブデモは問題なく成立するが、恒久的な集計は本番でSupabase移行後に対応）。

## エラー時の挙動

| 状況 | 挙動 |
| --- | --- |
| 検索クエリが空 | 検索ボタンを無効化する、または「クエリを入力してください」と表示 |
| ヒットなし（反対側でBM25が0件かつベクトル類似度が基準値未満。ADR-0008） | **空白画面にはしない。** 関連度%の低い候補カード（ベクトル検索の上位N件）は表示し続けたうえで、リスト上部に言い換え候補を提示（F-21）。裏側では`hit_count=0`で記録し、タブ②「まだ社内に無い技術」に個別の1件として追加 |
| 一言要約（`summary_plain`）が生成されていない | 要約欄を非表示にし、原文のみ表示する（固定文へのフォールバックはしない） |
| `patents`/`tech_usage`/`evaluations`に該当データがない | 「登録なし」「実績なし」「未評価」と明示し、空欄のまま隠さない（データがないことも判断材料になる） |
| FTS5索引と`documents`の内容が不整合 | 登録・更新時のトリガーで同期する。整合チェックをテストで確認（実装メモ） |
| Streamlit再起動で`search_logs`が消える | デモ当日はアプリ起動中のみ有効という前提を手順書に明記。恒久集計は対象外（N-04） |
| 埋め込みモデルへの入力が空文字・極端に長い | 512トークンを超える場合はチャンク分割済み文書として処理。空文字は検索実行前にUIで防ぐ |

## 決定事項（ADRのIDだけ書く。中身はADR.md）
- ADR-0002 / ADR-0003 / ADR-0004 / ADR-0005（ハイブリッド検索・RRF・SQLite保存・埋め込みモデル）
- ADR-0006（双方向検索のロジック）
- ADR-0007（過去の使われ方の再定義）
- ADR-0008（ヒットなし判定の方式）
- ADR-0009（立場切替ボタン）
- ADR-0010（関連度スコアの表示方式）
- ADR-0011（Janome＋品詞フィルタでのトークナイズ）
- ADR-0012（シーズ収集はINPIT開放特許DBで候補探し→特許公報から少数手動転記）
- ADR-0015（データの再現性と共有方針）
- ADR-0016（シーズ側技術文書への一言要約の実装）
- ADR-0017（画面構成は検索タブ＋技術データベースタブの2つ）
- ADR-0018（一致理由ハイライトの語句ベクトルは都度計算。事前保存しない）
- ADR-0019（訴求の競合チェック・空き地判定は今回のPoCでは対応しない）
- ADR-0020（要約はfixtures準備時に生成済み、埋め込みは起動のたびに再計算）
- ADR-0021（背景技術・課題も転記し、フィールドごとに別ベクトル化して最大類似度を採用）
- ADR-0022（評価・見送り記録の`evaluations`テーブルを新設。デモ用の架空データ）
- ADR-0024（同じ側の検索を復活。反対側の下に別枠で表示）
- ADR-0025（手入力データはknowledge.dbで編集し、seeds.sqlへ書き出してコミット）
- ADR-0023（表示順は関連度の降順。RRFは候補集合の決定にのみ使う）

## 未決
- ADR-0001：ROIの具体的な数値（探索時間削減率・投資額・回収期間）
- 「ヒットなし」判定の基準値（暫定0.80）、関連度スコアの変換式、埋め込みモデルの最終選定（e5 vs
  ruri）はデータ投入後に確定
- INPIT開放特許DBで検索する化粧品関連キーワード、転記する特許の件数・担当（最優先）
- INPIT開放特許DBで検索する化粧品関連キーワード、転記する特許の件数・担当（最優先）
